# GitHub Pages 公开版 v0.2.0

这是脱敏后的静态比赛展示包，不包含 Python 源码、企业接口、内部调研截图、真实门店数据或生效中的业务阈值。

更新 GitHub Pages：

1. 打开公开仓库 `revenue-workflow-ai-factory-demo`。
2. 选择 **Add file → Upload files**。
3. 将本文件夹中的所有文件和 `data` 文件夹一起上传到仓库根目录。
4. 选择 **Commit changes**。
5. 等待 GitHub Pages 自动更新。

新增页面：`pricing-knowledge.html`。它展示 Signal → L1 → L2 → L3、主因/确认/否决结构、动作边界和 Trace。

重要说明：公开版是固定数据演示，不会分析任意新 Case，也不会执行真实调价。
